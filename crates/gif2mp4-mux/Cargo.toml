[package]
name = "gif2mp4-mux"
version.workspace = true
edition.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
muxide = { version = "0.2", default-features = false }
wasm-bindgen = "0.2"
js-sys = "0.3"
